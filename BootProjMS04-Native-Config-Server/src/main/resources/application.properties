spring.application.name=BootProjMS03-External-Config-Server

# Server Port
server.port=8888

#activate native profile
spring.profiles.active=native

#specify "config" folder classpath as Native Config Location for configuration server
spring.cloud.config.server.native.search-locations=classpath:/config
